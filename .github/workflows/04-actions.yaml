name: 04 build action
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
      - name: Install dependencies
        working-directory: 04-actions/react-app
        run: npm ci
      - name: Run tests
        working-directory: 04-actions/react-app
        run: npm run test
